## 2026-02-24

**Fix: Modifier key interference when gesture triggers use modifier keys**
- Gestures activated with modifier keys (e.g. Ctrl+drag) caused held modifiers to
  bleed into the action's keyboard shortcuts (e.g. switch spaces, App Exposé)
- Root cause 1: `releaseAllModifierKeys()` used `.privateState` which only cleared a
  private event source table, not the system-wide HID state.
- Root cause 2: Keyboard shortcuts only set modifier flags on the main key event but
  never sent actual modifier key-down events. System-level shortcuts (Ctrl+Arrow for
  spaces, etc.) check the global modifier state, not just event flags.
- Fix: Added shared `postKeyboardShortcut()` utility that sends the full sequence:
  release all modifiers → modifier key-down → main key down/up → modifier key-up.
  Uses `.hidSystemState` so events update the system-wide modifier state (required for
  system shortcuts like Ctrl+Arrow). Sets `localEventsSuppressionInterval` to temporarily
  suppress physical keyboard input so held gesture-trigger keys can't interfere.
  Switched CoreActionsPlugin `moveToSpace`/`activateAppExpose` from AppleScript to
  CGEvent path. All keyboard shortcut implementations now use `postKeyboardShortcut()`.

## 2026-02-21

**Refactor: Gesture Model — Removed GestureTrigger, Flattened API**
- Removed `GestureTrigger` struct; `zone`, `modifiers`, and `dragModifier` are now computed
  properties directly on `Gesture` (derived from `components`)
- Removed deprecated `ActivationSettings` struct; moved to private `LegacyActivationSettings`
  used only inside `Gesture.init(from:)` for backward-compatible Codable migration
- `components: GestureActivationComponents` changed from optional to non-optional stored property
- `genericActivation` promoted to primary stored property (was secondary/optional)
- `Gesture.init(from:)` now handles migration from legacy `trigger` and `activation` coding keys
- Removed `GenericActivation.init(from legacy: ActivationSettings)` migration initializer
- Removed `GestureActivationComponents.init(fromLegacyGesture:)` and `toLegacyActivationType()` helpers
- Updated all callers: `ActionExecutionManager`, `GesturesView`, `GestureConfigurationSheet`,
  `DebugReportService`, `GestureConfigurationService`, `ZoneHighlightWindow`, `ActivationMapper`

**Fix: Disabling Profile Shortcut No Longer Deletes It**
- Added `keyboardShortcutEnabled` field to `ConfigurationProfile` (optional backing store
  for backwards compatibility; existing profiles default to enabled)
- `toggleShortcutEnabled` now only persists the enabled flag — the shortcut value is
  preserved in the profile even when disabled
- `KeyboardShortcutDetectorPlugin` checks `keyboardShortcutEnabled` before triggering
  a profile switch, so disabled shortcuts are ignored at the detection level
- `loadProfileData()` initializes `shortcutEnabled` from `profile.keyboardShortcutEnabled`
  so the correct state is restored on profile switch or app restart

**Fix: UI Response Delay Eliminated**
- `UIServices.loadData()` and `ProfileManagementService.loadProfiles()` now skip the
  `DispatchQueue.main.async` hop when already on the main thread
- All UI-triggered actions (add, update, delete, rename, etc.) now update `@Published`
  properties synchronously, removing the ~¼s delay between action and UI update

---

## 2026-02-17 (continued)

**Feature: Inline Shortcut in Profile Header**
- Removed separate "Quick Switch Shortcut" section card from ProfileDetailEditor
- Shortcut now appears inline in the header row next to the profile name
- Idle state: keyboard icon + shortcut text (or faded "Add shortcut" if none)
- Edit state: `KeyboardShortcutFieldView` + Clear link + ✓ done button appear in-place
- Toggled via `editingShortcutInline` state; saves immediately via `onChange(of: editingShortcut)`
- Profile loads/reloads shortcut in `loadProfileData()` on profile switch

**Settings: Detection Flat Grouped Layout**
- Replaced subcategory-based detection rendering (segmented picker) with a single flat `SettingsEntry`
- `DetectionPluginSettingsProvider` now emits one entry; `DetectionFlatSettingsView` renders all items
- Items partitioned into three labeled sections: General, Zone Detection, App Profiles
  - App Profiles: all `AppConfigurationDetectorPlugin` settings
  - Zone Detection: `.detection` + `.appearance` category settings from any plugin
  - General: remaining settings (`.performance`, etc.)
- Section labels are small uppercase headers between groups
- `DetectionSubcategories` enum removed (no longer needed)
- Detection category in Settings sidebar is now a single flat page, no subcategory picker

---

## 2026-02-17

### Profiles & Settings Polish

**Feature: Checkboxes for Profile Selection**
- Each ProfileListRow now has a visible checkbox (circle icon) at left
- Clicking the checkbox toggles selection (cmd+click behavior)
- Checkbox fades in on hover when not selected, always visible when selected
- Cmd+click and shift+click multi-selection still works as before

**Feature: Shortcut Editing in Profile Detail**
- ProfileDetailEditor now shows a "Quick Switch Shortcut" section
- KeyboardShortcutFieldView embedded directly — record or clear shortcut without opening a sheet
- Saves immediately on change via `updateProfileKeyboardShortcut`
- `loadProfileData()` also initializes the shortcut state when switching profiles

**Fix: Remove Import Templates Confirmation Sheet**
- Removed ImportResultsSheet — no longer shown after template import
- Import now shows inline status message (success or partial failure with name conflicts)
- Auto-dismisses after 1.5s on full success; stays open on partial failure for user to see errors
- Calls `UIServices.shared.loadData()` after import to refresh profile list

**Settings: Combine Data Management + About**
- Removed separate `dataManagement` category from SettingsCategories
- Both Data Management and About content now live under the single "About" category
- New `AboutSubcategories.info` and `AboutSubcategories.data` subcategory descriptors
- Settings sidebar loses one entry; content accessible via segmented subcategory picker

---

## 2026-02-12

### UI Improvements: Generic Trigger System, Profiles, App Profiles

**Refactor: Fully Generic Trigger Display & Duplicate Detection**
- Gesture `triggerKey` now delegates entirely to `GestureActivationComponents.triggerKey`
  which iterates all registered components generically — no hardcoded trigger types
- Gesture `displayDescription` now uses `components.previewString` — no hardcoded types
- GesturesView `triggerSummary` uses `components.previewString` instead of checking each type
- GesturesView expanded detail iterates `enabledComponentDetails` via ForEach instead of
  manual per-type if-checks
- New on GestureActivationComponents:
  * `allComponents` — all registered (label, config) pairs in display order
  * `enabledComponentDetails` — only enabled components as (label, value) pairs
  * `triggerKey` — deterministic duplicate-detection key built generically
  * `previewString` now built from `enabledComponentDetails`
- Main app code no longer knows about specific trigger types for display or dedup

**Feature: Profile Multi-Selection**
- Cmd+click to toggle individual profiles in/out of selection
- Shift+click to range-select multiple profiles
- Selection bar with batch Duplicate/Export/Delete buttons
- Multi-selection detail view with aggregate stats and batch actions
- Overflow menu updated with batch operations on selected profiles

**Feature: Editable Gesture List in Profile Detail**
- Profile detail shows full editable gesture list:
  enable/disable toggle, edit (opens config sheet), delete with confirmation,
  double-click to edit, right-click context menu
- "Add Gesture" button to add gestures directly to the profile
- New `updateProfileGestures()` in UIServices
- New EditProfileGestureSheet, AddProfileGestureSheet wrappers

**Fix: App Profiles Card Margins**
- `UnifiedAppRuleRow` now uses consistent spacing with other card rows:
  `MGStyle.Spacing.lg` for HStack spacing, `.xs` for text VStack,
  `.md` for vertical padding (was `.sm`), removed fixed `minWidth` on text

**Profiles: Double-Click Sets Active**
- Double-clicking a profile row sets it as active profile
- Removed pencil/rename button from hover actions (rename is in detail editor)
- Context menu: Set Active, Duplicate, Delete

### UI Polish Pass: App Profiles, Gesture Config, Profile Management

**App Profiles Tab:**
- Sort all rules alphabetically (localizedCaseInsensitiveCompare)
- Unified rule list: mappings and disabled apps in a single sorted list
- Each row has a unified dropdown with all profiles + "Disable Gestures" option
- Switching between profile/disable is seamless via the dropdown
- Reduced card row padding/buffer (smaller icons 28px, tighter vertical spacing)
- Added "Browse..." button in Add Rule sheet to select apps from Finder (NSOpenPanel)
- Add Rule sheet: radio buttons with inline profile dropdown
  - "Use specific profile: [Dropdown]" (inline)
  - "Disable gestures" (radio)
- Custom RadioButton component for native-looking radio controls
- Selected application shown at bottom of app selector with icon + name + bundle ID
- Right-click context menus on all rule rows

**Gesture Configuration Sheet:**
- Gesture preview moved into the footer bar as plain text
- Format: "Trigger1 + Trigger2 → Action Name"
- No separate section — shares the same footer as Cancel/Save buttons
- Removed pill-based preview in favor of simple text

**Gesture Cards (Expanded View):**
- Added parameter type/format hints (text, bool, int, number, list, json)
- Type hints shown as small monospaced badges next to parameter values
- Improved formatting for array parameters ("3 items") and dict parameters ("2 fields")

**Profiles Tab:**
- Active profile deletion now works correctly:
  - Checks Configuration.activeProfileId directly (not stale @Published value)
  - ProfileManagementService guard fixed to use configuration.activeProfileId
  - Creates replacement "Default" profile if last profile deleted
  - Verifies switch completed before attempting deletion
- "Import from Template" button added to Create New Profile sheet footer (bottom-left)
- Removed separate template/add buttons from profile list footer

**To-Do Completed:**
- [x] App Profiles: Add button to select application from finder
- [x] App Profiles: Sort profile mappings alphabetically  
- [x] App Profiles: Move select profile in line with rule type
- [x] App Profiles: Buffer on card buttons could be reduced
- [x] App Profiles: Dropdown includes disable option alongside profiles
- [x] Gesture Config: Show gesture preview [Trigger] -> [Action] in footer as text
- [x] Gesture Cards: Show parameter information (Format)
- [x] Profiles: Allow deletion of active profile (fixed service-layer guards)
- [x] Profiles: Import from Template button in new profile sheet footer

---

### UI Unification: Hit Targets, List Styling, and Performance

**New UIStyles Components:**
- **MGActionButton**: Icon button with expanded 28×28 hit target (up from ~11px icon-only). 
  Replaces all raw icon buttons for row actions across the app.
- **MGListCardModifier (.mgListCard)**: Unified card styling for list items with consistent 
  hover highlights, subtle borders, and expansion state. Used for gesture cards, service rows.
- **MGListRowModifier**: Added `showBorder` parameter for optional selection accent border.
- **MGRowActions**: Now uses MGActionButton internally; added `help` tooltip support per action.

**Files Updated (7):**
- **UIStyles.swift**: Added MGActionButton, MGListCardModifier, updated MGListRowModifier 
  and MGRowActions.
- **GesturesView**: Gesture cards use `mgListCard` modifier, `MGRowActions` with expanded 
  targets, `MGActionButton` for expand chevron.
- **ProfilesView**: Profile list rows use `MGActionButton`, `mgListRow(showBorder:)`, 
  switched to LazyVStack for performance.
- **SavedActionsView**: Rows use unified `mgListRow`, expanded checkbox hit target (28×28), 
  full-row click now toggles selection (not just checkbox).
- **AppProfilesView**: Mapping/disabled rows use `mgListRow` and `MGActionButton`, 
  switched sections to LazyVStack.
- **ServicesView**: Service rows use `mgListCard` and `MGActionButton` for configure button.
- **DeveloperView**: Plugin rows use `MGActionButton` (horizontal layout), log file delete 
  uses `MGActionButton`.

**To-do Items Resolved:**
- ✅ Extra clickable area around all icon/hover buttons (28×28 hit targets)
- ✅ Unified list styles across all tabs (mgListCard/mgListRow modifiers)
- ✅ Unified styles between plugin management and service management
- ✅ Saved actions: clicking anywhere on card toggles selection
- ✅ LazyVStack for performance in profiles, app profiles, and other list views
- All hover animations now use consistent 0.15s easeInOut timing

---

## 2026-02-11 (3)

### UI Improvements: Responsiveness, Compactness, and Polish

**Sheet Footer Buttons**
- MGSheetFooter now supports `cancel:` parameter to place Cancel directly next to primary action
- Updated all sheets (Gesture, Saved Action, Profile, Template Import, Profile Picker)
- Cancel buttons grouped with Save/Add/Select for clearer visual pairing

**Hover Button Responsiveness**
- Changed all hover-revealed action buttons from conditional rendering (`if isHovered`) to
  opacity-based (`opacity: isHovered ? 1 : 0`). Eliminates layout recalculation that caused
  click delays. Applied across: SavedActionsView, AppProfilesView, GesturesView, ProfilesView.

**Gestures Tab**
- Profile switcher moved from header to dedicated bar below title with gesture stats
- Delete button on gesture cards now always present (opacity-based, visible on hover)

**Gesture Configuration Sheet**
- ComponentToggleCard compacted: single-line layout, mini toggle, description as trailing hint
- All sub-config views (modifiers, zone, drag, mouse, keyboard) reduced padding/borders
- Mouse button picker: changed from segmented to menu, added "none" option
- Keyboard shortcut: inline error indicator instead of multi-line warning
- Screen zone: removed redundant labels, cleaner picker layout

**Saved Actions Tab**
- Selection indicator changed from circle checkmarks to square checkboxes
- Hover actions use opacity approach for instant click responsiveness

**Profiles Tab**
- Profile list rows: added hover actions (set active, duplicate, delete) with opacity transition
- Keyboard shortcut displayed inline in profile list rows
- Double-click on profile row sets it as active
- Profile detail header: gesture count, active count, keyboard shortcut, and modified date
  all displayed in info summary row. Removed redundant keyboard shortcut from gesture card.
- Empty state for profiles with no gestures

**App Profiles**
- Replaced edit sheet with inline profile dropdown on each app mapping row
- Removed EditAppRuleSheet and EditDisabledAppSheet entirely (~130 lines removed)
- DisabledAppRow simplified to just show badge and delete button

**Zone Highlights**
- Fixed labels overflowing thin edge zones: smaller font for narrow zones (<50px),
  position clamped within bounds, text truncates if too wide, semi-transparent
  background pill for readability

---

## 2026-02-11 (2)

### Major UI Overhaul: Gestures, Profiles, and Configuration

**GesturesView — Replaced HSplitView with Rich Card Layout**
- Removed the split-view pattern (redundant read-only detail panel)
- Each gesture is now a rich card with inline trigger pills showing zone, modifiers,
  drag type, keyboard, mouse button, timing at a glance
- Cards expand/collapse to reveal full trigger, action, and timing details
- Added MGZoneIndicator — compact 3×3 grid showing the active zone inline
- Added MGTriggerPill — color-coded inline pills for each trigger component
- Full right-click context menu on each card (Edit, Enable/Disable, Delete)
- Double-click to edit, single-click to expand/collapse
- Delete confirmation dialog before removing gestures
- Improved header subtitle shows active/total count

**ProfilesView — Inline Editing in Detail Panel**
- Profile detail panel is now an editor, not a read-only view
- Inline name editing (click pencil icon, edit, confirm)
- Zone detection sliders (edge threshold, corner size, corner buffer) edit directly
- Haptic feedback toggle directly in the detail panel
- Gesture overview shows compact list with zone indicators and action names
- Stat bubbles showing Total/Active/Disabled counts
- Quick action bar (Duplicate, Export, Delete) in the detail panel
- Removed redundant edit sheet — most editing now happens inline
- Profile list rows now use context menus instead of hover-only actions
- Active profile indicator dot in list rows

**GestureConfigurationSheet — Visual Improvements**
- New visual zone picker (MGZonePicker): interactive 3×3 grid representing screen
  edges and corners, replacing the dropdown-only selector
- Dropdown still available as secondary picker alongside the visual grid
- Selected zone name displayed prominently with accent color
- Gesture preview card redesigned with trigger pills + action pill instead of
  monospaced text — shows real-time visual preview of the gesture configuration
- Modifier key toggles redesigned as visual key-cap buttons (⌘ ⌃ ⌥ ⇧) with
  labels and active state highlighting
- Active modifier combination shown as text below the key buttons

**New Reusable Components (UIStyles.swift)**
- MGZonePicker: Interactive 3×3 grid for selecting screen zones with visual feedback
- MGZoneIndicator: Compact non-interactive zone position indicator
- MGTriggerPill: Color-coded inline pill for displaying trigger components
- Added MGStyle.Corner.xs (2pt) for fine-grained corner radii

**Files Changed**: UIStyles.swift, GesturesView.swift, ProfilesView.swift,
  GestureConfigurationSheet.swift

---

## 2026-02-11

### UI Improvements: Headers, Detail Panels, and Action Selection
- **New shared components added to UIStyles.swift**:
  * `MGDetailSection` — modern card-style detail section replacing GroupBox in detail panels
  * `MGDetailRow` — consistent key-value row with optional icon for detail panels
  * `MGSelectionBanner` — prominent banner showing currently selected item with checkmark
  * `MGCompactHeader` — unified tab header with title/subtitle, primary actions, and overflow menu
  * `MGMenuItem` — menu item model for MGCompactHeader's overflow menu
- **Tab header redesign (decluttered, consistent)**:
  * GesturesView: Profile info moved to subtitle, reset/clear in overflow menu, search toggleable
  * ProfilesView: Import/export/templates moved to overflow menu, search toggleable
  * SavedActionsView: Edit/remove/import/export in overflow menu, search toggleable
  * AppProfilesView: Switched to MGCompactHeader for consistency
  * ServicesView: Cleaned up content header, removed redundant install button
- **Gesture detail panel modernized**:
  * Replaced GroupBox with MGDetailSection for Trigger, Action, and Timing sections
  * Uses MGDetailRow for consistent key-value display with icons
  * Added inline action badges (enabled status, action name) in title area
  * Edit/delete actions moved to inline title bar buttons
  * Shows parameters when configured
- **Profile detail panel modernized**:
  * Replaced GroupBox with MGDetailSection for Overview, Zone Settings, Gestures
  * Uses MGDetailRow with icons throughout
  * Consolidated action buttons into title bar with overflow menu
  * Shows up to 8 gestures in summary (was 5)
- **Action selection improved**:
  * Added MGSelectionBanner showing selected action name + description prominently
  * Covers plugin actions, saved actions, and unknown actions with different colors
- **Double-click to edit**: Added to gesture rows and profile rows
- **Search toggle**: Search bars hidden by default on Gestures, Profiles, SavedActions tabs;
  revealed via magnifying glass button to reduce visual clutter
- **Resolved to-do items**: Different UI styles, better gesture info, action selection indicator,
  reduce header crowding, consistent headers, search bar cleanup

### Unified UI Styles Throughout Application

**Extended MGSidebar with header/footer slots** - Added optional `header` and `footer` ViewBuilder parameters to MGSidebar, enabling views like Settings to embed search fields and toggles within the sidebar while using the shared component.

**Structural consistency across sidebar views:**
- SettingsView sidebar now uses MGSidebar instead of manual layout
- ServicesView refactored from redundant MGPageHeader+sidebar to sidebar+MGSectionHeader pattern (matching DeveloperView)
- All three sidebar-based views (Settings, Services, Developer) now follow the same layout pattern

**Replaced hardcoded values throughout:**
- Padding values (5, 10, 14, etc.) → MGStyle.Spacing constants
- Font sizes (9, 10, 12, 13) → MGStyle.FontSize constants  
- Colors (Color.gray.opacity(0.3), inline Color.accentColor.opacity(0.1)) → MGStyle.Colors constants
- Manual "Advanced" badge styling → MGBadge component
- Manual empty state placeholders → MGEmptyState component

**Consistent headers:**
- Detail view headers in GesturesView and ProfilesView now use MGSectionHeader
- PluginSettingsView header uses MGSectionHeader
- TabManager views (PluginTabManagerSettings, PluginManagerView) use MGSectionHeader

**Fixed MGContentCard** internal spacing from hardcoded `15` to `MGStyle.Spacing.xl`.

**Files changed:** UIStyles.swift, SettingsView.swift, ServicesView.swift, DeveloperView.swift, GesturesView.swift, ProfilesView.swift, PluginSettingsView.swift, TabManager.swift

---

## 2026-02-08

### Major Refactor: Modular Activation Component System

**Separated activation types into independent, modular components**
- Modifiers are now their own activation type (separate from zones)
- Drag type is now its own activation type (separate from zones and buttons)
- Screen zones, modifier keys, drag types, mouse buttons, and keyboard shortcuts are all independent
- Each component can be enabled/disabled independently

**New Component Architecture:**
- Created `ActivationComponents.swift` with component configuration system
- `ActivationComponentConfig` protocol for component configurations
- Concrete configs: `ModifierKeyConfig`, `ScreenZoneConfig`, `DragTypeConfig`, `MouseButtonConfig`, `KeyboardShortcutConfig`
- `GestureActivationComponents` struct stores all component configs for a gesture
- Added `components` field to `Gesture` struct (optional during transition for backward compatibility)

**Dynamic UI Generation (Foundation)**
- Added `ActivationComponentUIMetadata` with field specifications
- Extended `ActivationProvider` protocol with `getComponentUIMetadata()` method
- Created `ActivationComponentService` to query plugins for UI metadata
- UI is now modular and extensible (ready for plugin-provided metadata)

**Completely Rewrote GestureConfigurationSheet:**
- Now uses component-based configuration instead of hardcoded activation types
- Each component has a toggle card (with icon, title, description)
- Configuration views show/hide based on which components are enabled
- Clean visual hierarchy with colored cards for each component type
- Preview shows all enabled components in a readable format

**Example Usage Scenarios Now Possible:**
- Zone only (no modifiers): Click in top-right corner triggers action
- Modifiers only (no zone): Hold Cmd+Ctrl anywhere triggers action
- Zone + Modifiers: Hold Cmd+Ctrl and click top-right
- Drag only: Drag with left button (no zone or modifiers required)
- Any combination of the 5 component types

**MouseButtonTrigger Enhancements:**
- Added `.none` and `.any` options to `MouseButton` enum
- `.none` is now the default (no mouse button required)
- `.any` matches any button press
- Updated `DragModifier.from(mouseButton:)` to handle new cases

**Backward Compatibility:**
- All existing gestures continue to work
- Legacy `GestureTrigger` and `ActivationSettings` still present
- Automatic migration from legacy format to components via `GestureActivationComponents(fromLegacyGesture:)`
- Conversion back to legacy format via `toLegacyActivationType()` 
- `updatingComponents()` method creates new gesture from updated components

**Code Organization:**
- New `Detection/ActivationComponents.swift` file (component definitions)
- New `Services/ActivationComponentService.swift` file (component query service)
- Extended `ActivationProvider` protocol in `ActivationCoordinator.swift`
- Completely rewrote `UI/GestureConfigurationSheet.swift` (now component-based)

**Future Work:**
- Detection plugins will provide their own UI metadata via `getComponentUIMetadata()`
- UI will become fully dynamic, building itself from plugin metadata
- Plugins can add new activation types by implementing the protocol
- No more hardcoded activation types in UI code

This refactor makes the activation system truly extensible and modular, setting the foundation for plugin-provided UI components and custom activation types.

### UI Improvement: Enhanced Add/Edit Gesture UI - Better Activation Method Selection

**Redesigned activation method selection for improved usability**
- Replaced confusing dropdown menu with intuitive toggle-based selection
- Users now select individual trigger types (Gesture, Keyboard, Mouse Button) via switches
- Activation type is automatically computed from enabled combinations
- Each trigger method has its own card with:
  * Clear icon and title (hand.draw, keyboard, computermouse)
  * Descriptive text explaining what the method does
  * Visual highlight when enabled (blue border + background tint)
  * Toggle switch for easy enable/disable

**Improved visual organization**
- Prominent preview card at top showing complete gesture configuration
- "Trigger Methods" section with visual cards for each type
- "Trigger Configuration" section dynamically shows only relevant fields
- Smooth animations when toggling trigger types on/off
- Better spacing and visual hierarchy throughout

**Enhanced trigger configuration**
- Each trigger type has its own styled card with colored icon
- Configuration fields are left-aligned and well-spaced
- Required field validation with visual warnings
- Empty state message when no trigger methods are selected

**Moved Enabled toggle**
- Relocated from "Activation Method" section to header
- Now appears as a switch next to the Cancel button
- More prominent and easier to access

**Better footer**
- Removed preview text (now in prominent card at top)
- Cleaner two-button layout: Cancel and Save/Add
- Save button is styled as prominent with proper disabled state

**Window sizing**
- Increased width to 750px (from 700px)
- Increased height to 680px (from 650px)
- More breathing room for the enhanced UI

### Feature: Zone Highlights Preview Button
- **Added "Preview Zone Highlights" button in Detection Plugin Settings**:
  * New button appears in the Detection category alongside zone size settings
  * Clicking the button shows all screen zones with labels for 5 seconds
  * Provides immediate visual feedback when adjusting zone dimensions
  * Works independently of modifier keys — shows all zones immediately
- **Implementation**:
  * Added button setting to ScreenZoneDetectorPlugin's settingsDefinitions
  * New `showPreview(duration:)` method in ZoneHighlightManager
  * Preview displays all 8 zones (corners + edges) in semi-transparent overlay
  * Auto-hides after specified duration (default 5 seconds)
  * Zone names displayed as labels during preview
- **User Experience**:
  * Helps users visualize zone sizes before committing changes
  * Makes it easy to test different Edge Threshold, Corner Size, and Corner Buffer values
  * No need to hold modifier keys or set up gestures to see zones
  * Clean, temporary preview that doesn't persist settings

### Refactor: Removed Gesture-Awareness from Detection Plugins
- **Problem**: Detection plugins were violating single responsibility by understanding
  gesture structure through `gestureUsesActivation()` method. Plugins had to inspect
  gesture fields (modifiers, dragModifier, activation types) to determine if they
  were needed, creating tight coupling between detection layer and gesture configuration.
- **Solution**: Introduced **ActivationMapper** — a central service that understands
  gesture structure and maps gesture properties to activation types.
- **Changes**:
  * New file: ActivationMapper.swift containing all gesture→activation mapping logic
  * ActivationCoordinator now uses ActivationMapper instead of asking plugins about gestures
  * Removed `gestureUsesActivation()` from ActivationProvider protocol
  * Removed `validateGate()` from ActivationProvider protocol
  * Added `getGateValidationMetadata()` to ActivationProvider protocol
  * Plugins now only provide detection metadata, not gesture interpretation
  * Precision gate validation moved to coordinator using mapper
- **Refactored Plugins**:
  * ModifierKeyDetectorPlugin: Removed gesture inspection, provides modifier state metadata
  * MouseButtonDetectorPlugin: Removed gesture inspection, provides button state metadata
  * ScreenZoneDetectorPlugin: Removed gesture inspection
  * KeyboardShortcutDetectorPlugin: Removed gesture inspection
  * AppConfigurationDetectorPlugin: Removed gesture inspection
- **Benefits**:
  * True separation of concerns: plugins detect, mapper interprets
  * Reduced coupling: plugins don't depend on Gesture structure
  * Better testability: clear boundaries for unit tests
  * Easier maintenance: all gesture interpretation in one place (ActivationMapper)
  * Changes to Gesture fields only affect mapper, not all plugins
- **Note**: Internal plugin optimizations (like checking if drag gestures exist before
  installing monitors) are still fine — that's implementation detail, not architectural
  responsibility violation. The key improvement is that plugins no longer need to
  understand *what makes a gesture use their activation type*.
- **Follow-up Fix**: Discovered and fixed additional gesture structure inspection:
  * GestureLookup was checking `gesture.activationType` — now uses ActivationMapper
  * KeyboardShortcutDetectorPlugin was filtering by `gesture.activationType` — now uses ActivationMapper
  * MouseButtonDetectorPlugin was filtering by `gesture.activationType` — now uses ActivationMapper
  * ActivationMapper is now the **single source of truth** for all gesture→activation mapping
  * ZERO direct `gesture.activationType` checks remain in detection layer

## Unified UI Styles Throughout Application
- **Goal**: Eliminate all hardcoded UI values (colors, spacing, padding, corner radii, font sizes)
  and replace with centralized MGStyle constants from UIStyles.swift.
- **Files Updated (10)**:
  * AppProfilesView: Full rewrite — replaced custom header/search/empty state/badges/rows with
    MGPageHeader, MGSearchField, MGEmptyState, MGBadge, MGSheetHeader, MGSheetFooter, MGRowActions,
    MGListSectionHeader, MGStyle.Colors/Spacing/Corner throughout.
  * GestureConfigurationSheet: MGSheetHeader/MGSheetFooter replace custom header/footer;
    all hardcoded values replaced with MGStyle constants.
  * SavedActionConfigurationSheet: MGSheetHeader/MGSheetFooter; unified spacing/padding.
  * ServicesView (ServiceConfigurationSheet, ServiceInstallSheet): Unified sheet components.
  * ActionSelectionView: MGSearchField replaces custom search; all Color(NSColor.*) → MGStyle.Colors;
    raw values → MGStyle constants.
  * BundleEditorView: All Color(NSColor.*) → MGStyle.Colors; raw values → MGStyle constants.
  * PluginSettingsView: NSColor refs → MGStyle.Colors; hardcoded values → MGStyle constants.
  * BuiltInSettingsCategories: All hardcoded font sizes, spacing, padding, corner radii
    replaced with MGStyle constants across all settings views and helpers.
- **Result**: 914 fewer lines (2582 removed, 1668 added) — cleaner, more consistent codebase.
  All UI files now exclusively reference MGStyle for visual constants.

## Bug Fixes and UI Improvements

### Fix: Quick Profile Switch Now Works
- **Problem**: Profile switching via gesture actions (Next Profile, Previous Profile, Switch to Profile)
  silently failed — profile didn't persist and UI didn't update.
- **Root Causes**:
  1. `SandboxedPluginContext.applyProfile()` called `Configuration.applyProfile()` without
     `setAsDefault: true`, so profile change didn't persist across sessions.
  2. Built-in plugin permissions blocked posting "GestureConfigurationChanged" notification,
     so UI never received the update signal after a switch.
- **Fixes**:
  * `SandboxedPluginContext.applyProfile()` now passes `setAsDefault: true`
  * Added "GestureConfigurationChanged" to `allowedNotificationsToPost` in builtIn permissions

### Fix: Preview Zones Button Shows Zone Names Regardless of Label Setting
- **Problem**: Preview zones button only showed labels when `showZoneLabels` was enabled globally.
- **Solution**: Added `forceShowLabel` flag to `ZoneView`. Preview mode sets this flag to always
  display zone names (e.g. "topLeft", "bottom") independently of the global `showZoneLabels` setting.
- Zone names are never replaced by action names during preview.

### Fix: Removed Duplicate Edit Button from Gesture Cards
- **Problem**: Expanded gesture cards showed two edit buttons — one in the hover actions row
  and another standalone "Edit" button in the expanded detail area.
- **Solution**: Removed the standalone Edit button from the expanded content area. The pencil
  icon in the row actions is sufficient.

### UI: Made Action Buttons Always Visible
- **Problem**: Edit, delete, and other action buttons on list rows were only visible on hover,
  making them hard to discover and inaccessible on trackpad-only setups.
- **Solution**: Removed `.opacity(isHovered ? 1 : 0)` from all row action buttons across:
  * GesturesView (gesture card row actions)
  * ProfilesView (profile list row actions, profile gesture row actions)
  * SavedActionsView (saved action row actions)
  * AppProfilesView (app mapping row actions)

### UI: Removed Zone/Haptic Settings from Profiles
- **Problem**: Zone detection settings (edge threshold, corner size, corner buffer) and haptic
  feedback toggle were shown per-profile, but these are global settings — not per-profile.
- **Solution**: Removed zone detection and haptic feedback controls from:
  * ProfileDetailEditor (inline editing view)
  * AddEditProfileSheet (create/edit profile dialog)
- These settings remain accessible in the global Settings tab.

### Fix: Reset to Defaults Now Only Affects Current Profile
- **Problem**: "Reset to Defaults" in the Gestures tab deleted ALL profiles and recreated
  them from templates — losing all custom profiles.
- **Solution**: `resetToDefaults()` now only replaces the active profile's gestures with
  the factory default gesture set. Other profiles remain untouched.
- Updated confirmation dialog text to reflect the new behavior.
