MOUSEGESTURES - TO DO
=====================
See MouseGestures_changelog.txt for completed items.

--------------------------------------
KNOWN ISSUES
--------------------------------------
- Set keyboard brightness (absolute value) is approximate — uses NX key-step simulation
  since no public/private API exists for direct keyboard backlight brightness control.
  Result may vary by step count; not bit-accurate.

--------------------------------------
FUTURE / PLANNED
--------------------------------------
- Customizable variables for parameter fields
    - Allow inputs and outputs to scripts with these variables
    - Example: {clipboard}, {frontmost_app}, {selected_text} usable in any parameter
- Adding plugins should not require any code changes (no registration line needed)
    - Install via 'Install Plugin' button in Developer tab
    - Test with TestActionPlugin and TestDetectionPlugin bundles
- Possible: Combine mouse click and mouse drag into a single input type
    - Evaluates to true whenever the corresponding mouse button is pressed
    - Same way screen zones are true whenever cursor is inside them
