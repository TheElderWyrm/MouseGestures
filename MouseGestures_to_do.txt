General:
- Remove Duplicate code, unify architectures
- Work to reduce CPU/Memory Usage

- Rework Add Gesture UI to respond to activation type (So, for example, if 'Keyboard Only' is selected then it hides the options related to other activation types 
Future Features
- Switch to Plugin Based Detection system. 

Actions:
- More System Control Actions- (Ex. Change settings, etc.)
- Not all actions work.

Bugs
- Zone Highlights can get stuck if modifiers are released during certain actions (Example, entering full screen)
- Window Actions targeting multiple windows doesn't work

Improve UI
- Go Back to pre-rendering to increase performance
- Selecting the zone configuration in settings doesn't autofill with selected value and the slider not properly select size (Jumps up when released).
	- Duplicated in zone config and detection settings
- Unify UI Styles throughout the app
- Better Zone Configuration- Replace with more visual menu.
- Places with side menus make the entire menu clickable instead of just the text.
- Split settings into general/Advanced
- More configuration options
- Split action menu into section tabs
- Split actions into cascading menus (Media, System Control, Window Control, etc)
