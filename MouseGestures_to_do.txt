- Work to reduce CPU/Memory Usage
- Reduce duplicate code, unify architecture whenever possible

General:
- Add an advanced seeing setting to enable developer mode.
- Issue with nested developer mode, advanced settings menu. <-- With advanced toggle "advanced menu" might be unnecessary (See above)
-Possible: Delete/remake the show zone highlights window instead of hiding to save memory.

Actions:
- Not all actions work.
- Clean up unneeded actions
- bundle actions vs execute bundle actions?

Bugs
- Efficiency system could be more exact (Example, not all modifier combinations result in gestures that need zone detection activation.)
- Window Actions targeting multiple windows does not work
- Drag Detection seems unreliable
- Prevent multiple gestures of the same modifiers to be created

Improve UI
- Bundle action UI needs to be improved. <-- Switch to siftUI
- Improve appearance of Show Zone Highlights
- Add search to settings
- Pre-render UI to increase performance
- Unify UI Styles throughout the app
- Better Zone Configuration
	- More visual menu
	- More configuration options