General:
- Remove Duplicate code, unify architectures
- Work to reduce CPU/Memory Usage
- Rework Add Gesture UI to respond to activation type (So, for example, if 'Keyboard Only' is selected then it hides the options related to other activation types) (Must be compatible with custom plugin system)
- Efficiency mapping doesn't seem to be working (CPU rises when mouse is moved regardless of modifiers)
-Switch to using Cmnd+Cntrl as default for full screen.

Actions:
- More System Control Actions
- (Ex. Change settings, etc.)
- Not all actions work.

Bugs
- [DONE] Saved actions doesn't allow for actions that require advanced configuration/opening a sheet (Example: Bundle actions)
- Zone Highlights can get stuck if modifiers are released during certain actions (Example, entering full screen)
- Window Actions targeting multiple windows doesn't work
- Bug: Won't display (first?) sheet when opening service sheet. Opening a different sheet then returning to the original sheet you were trying to open works.
- Drag Detection seems unreliable

Improve UI
- Better Zone Highlights
- Add search to settings
- Go Back to pre-rendering to increase performance
- Unify UI Styles throughout the app
- Better Zone Configuration
- Replace with more visual menu.
- Split settings into general/Advanced
- More configuration options
- Split action menu into section tabs
- Split actions into cascading menus (Media, System Control, Window Control, etc)
