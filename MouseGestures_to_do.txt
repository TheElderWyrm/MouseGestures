MOUSEGESTURES - TO DO
=====================
Action items also in: MouseGestures_actions.txt

--------------------------------------
BUGS
--------------------------------------
- Actions freeze app when running [FIXED 2026-03-15]

Window Targeting:
  - Does not work for some actions (works for minimize, not for adjust_size)
  - Targeting multiple windows does not work

UI:
  - Keyboard shortcut detection not working for gestures [FIXED 2026-03-15]
  - Profiles: pressing delete should not select the profile checkbox
  - Picker: selection "" is invalid / no associated tag — gives undefined results
  - Configurations not aligned between trigger types
  - Action search a bit weird (Maybe showing off the top of the action list, reset scroll?) 

--------------------------------------
IMPROVEMENTS
--------------------------------------
Architecture:
  - Reduce duplicate code, unify architecture whenever possible
  - Move window actions (close/minimize/maximize/fullscreen) from Core to Window Management plugin
  - Improve activation system
  - Drag does not need to be a separate trigger configuration than mouse button
  - Reduce size of modifier keys trigger configuration
  - Make Action View categories dynamic
  - Haptic feedback on activation not finish

UI:
  - Use full 'gestures' UI in profiles
  - Manage detection plugins under developer mode
  - Open application action should bring up Finder to select any application
  - Show add-to-bundle button even when a bundle action is selected (choose to add or update)
  - Setting to display notification on action activation


Features:
  - Add reset to template in gestures
  - Bundle actions with variables (e.g. Open URL From Clipboard)
  - Test adding external plugins for detection and actions
  - open_url: Show only browsers when selecting app + add 'default browser' option
  - run_script: add interpreter selection option
  - cascade: add option to resize windows or not
  - Improve templates/default configuration
  - 'Tab Navigation' template

Cleanup:
  - Remove dummy autoupdate
  - Clean up unneeded actions
