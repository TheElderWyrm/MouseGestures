MOUSEGESTURES - TO DO
=====================
--------------------------------------
FUTURE / PLANNED
--------------------------------------
- Customizable variables
    - Action to set a variable
    - Allow inputs and outputs to scripts with these variables
    - Example: {user_variable}, {clipboard}, {frontmost_app}, {selected_text} usable in any parameter
- Adding plugins should not require any code changes (no registration line needed)
    - Install via 'Install Plugin' button in Developer tab
    - Test with TestActionPlugin and TestDetectionPlugin bundles
    - Should fully integrate into the efficiency sytem
- Possible: Combine mouse click and mouse drag into a single input type for the user
    - Option: Evaluates to true and send notification whenever the corresponding mouse button is pressed. Same way screen zones are true whenever cursor is inside them
    - Option: Evaluate whether click of drag is needed internally.


- Rename: Execute Action Bundle -> Action Bundle
- Display Brightness not working. Simplify complicated fallback mechanisms.
- None mouse input not working. When selected should prevent activation of action sir any mouse button is pressed (True only when no mouse button is selected.
- Selecting None and a different mouse configuration button turns off Mouse Trigger
- Improve full screen (doesn't always work when buttons not visible <-- Currently falls back to a keyboard shortcut, some applications accept Cmnd + Ctrl + F, some accept Fn + F, some both. If only one fallback is used Cmnd + Ctrl + F is probably better?
- Actions repeating on hold should not display notifications on every repeat when notify on action is enabled.