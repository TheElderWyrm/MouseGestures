- Work to reduce CPU/Memory Usage
- Reduce duplicate code, unify architecture whenever possible

General:
- Drag Detection/click detection seems to not be working
	- Should maybe not be its own system. Instead it just exists as a combination of two activation methods: Mouse button and zone.
- More isolated activation methods (Modifiers should be separate from zones, etc.)
- Improve settings organization. [DONE]
- missing highlight zone color setting.

Actions (fix later):
- Not all actions work.
- Clean up unneeded actions
- bundle actions vs execute bundle actions?
- Open application should bring up finder to select any application
- Open File parameter should have a button to select from finder
- Full screen unreliable.
	-Fn + F works on some apps
	-Command + Control + F
	-Using Accessibility to click the full screen button doesn't work when buttons aren't shown (common when in full screen) 
- Window Actions targeting multiple windows does not work

Improve UI
- App selection UI (Maybe just select apps from finder)
- Improved gesture editing screen. Current trigger selection UI needs improvement
- Better gesture information (Base off current bundle actions selection.)
- Action selection needs better indication of what is currently selected/add a select button to confirm selection (To prevent accidental overriding of previously selected action)
- Improve appearance of Show Zone Highlights
- Add searchbar to settings [DONE]
- Pre-render UI to increase performance
- Unify UI Styles throughout the app
- Better Zone Configuration
	- More visual menu
	- More configuration options
- Installing plugins should be in developer mode (currently in Developer Settings > Plugin Management)